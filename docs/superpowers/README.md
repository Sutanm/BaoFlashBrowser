# 设计与实施记录归档

`specs/` 和 `plans/` 按日期保存当时的需求、方案、验证门和实施步骤。它们用于解释“为什么这样做”，不是当前 API、路径、依赖或待办清单。

当前开发请从 [`../README.md`](../README.md) 和 [`../modules/00-overview.md`](../modules/00-overview.md) 进入。历史记录与当前源码冲突时，以源码、`package.json`、CI 工作流和 `AGENTS.md` 为准；不要为了让历史计划“看起来最新”而改写已经发生过的决策过程。
