# BaoFlashBrowser 代码架构审计报告 — 高优先级处理事项

> 审计日期：2026-08-27
> 审计范围：全项目源码（src/、tests/、tools/、build/）
> 项目版本：v1.1.1

> 复核说明：2026-08-27 对照当前源码重新核验并完成首轮整改。`npm run check` 通过；
> `npm run lint` 为 0 error / 34 个既有 warnings；`npm test -- --run` 为81个测试文件、
> 514项测试全部通过。整改前 `npm run probe` 曾提示 main、renderer、webview-preload、
> userscripts 等5类构建产物过期。其具体影响是：直接运行依赖 `release/` 的 Electron smoke
> 可能仍在测试修改前的 main/preload/renderer 代码；用户脚本 smoke 还可能测试过期的
> `release/tests/userscript-runtime-preload.cjs` 或 admin module。必须先运行目标 smoke 对应的
> `tests/electron/build-*.mjs`/npm 脚本。整改后构建新鲜度已恢复为 `OK`；当前 probe 仅因本机
> 尚无应用配置和运行日志而保留2项环境失败。
>
> 2026-08-27 更新：classifyAddress() 已由 ipaddr.js 替换（B-014 移除）；
> shell.openPath() 已添加空字节校验；文件名清理函数已统一。
> 依赖版本：xstate 5.32.6、opencv-js 4.12.0。
> 下列表格保留审计时的原始发现，当前状态以文末“2026-08-27 实施结果”为准。

---

## 目录

- [第一章：项目整体概况](#第一章项目整体概况)
- [第二章：架构与设计问题检查](#第二章架构与设计问题检查)
- [第三章：性能风险点扫描](#第三章性能风险点扫描)
- [第四章：BUG 隐患、安全、健壮性问题](#第四章bug-隐患安全健壮性问题)
- [第五章：项目目录与包结构审计](#第五章项目目录与包结构审计)
- [优先级整改清单](#优先级整改清单)

---

## 第一章：项目整体概况

### 1.1 项目技术栈、框架、依赖组件

| 类别 | 技术选型 | 版本 | 说明 |
|------|----------|------|------|
| **运行时** | Electron | 11.5.0 (锁定) | 最后支持原生 PPAPI Flash 的版本，永远不升级 |
| **渲染引擎** | Chromium | 87 | Electron 11 内置 |
| **主进程语言** | TypeScript | ^5.5.0 | esbuild 打包为 CJS |
| **渲染进程** | React 18 | ^18.3.1 | Vite 打包，chrome87 target |
| **状态管理** | Zustand | ^5.0.0 | 轻量级 React 状态 |
| **数据持久化** | Dexie (IndexedDB) | ^4.4.4 | 浏览器端数据层 |
| **持久化存储** | electron-store | ^6.0.1 | 主进程 KV 存储 |
| **CSS 框架** | Tailwind CSS | ^3.4.0 | PostCSS + autoprefixer |
| **IPC 校验** | Zod | ^3.22.4 | 运行时类型验证 |
| **国际化** | typesafe-i18n | ^5.27.1 | 中文为主语言 |
| **测试** | Vitest | ^4.0.0 | 单元测试 + Electron 冒烟测试 |
| **Lint** | ESLint 9 (flat config) | ^9.0.0 | typescript-eslint + react-hooks |
| **Flash 运行时** | Ruffle | ^0.5.0 | Flash 仿真器（dev 依赖） |
| **自动化** | Blockly + xstate + OpenCV-js | 10.4.3 / 5.32.6 / 4.12.0 | 可视化工作流编程 |

### 1.2 项目目录结构解读

```
BaoFlashBrowser/
├── src/
│   ├── main/                          # Electron 主进程
│   │   ├── index.ts                   # 入口（200行）
│   │   ├── ipc/                       # 12 个 IPC 通道处理器
│   │   ├── modules/                   # 25 个核心模块
│   │   │   ├── automation/            # 9 文件（BrowserView 驱动、视觉工作线程）
│   │   │   ├── userscripts/           # 20 文件（管理器、解析器、匹配器、存储、GM API）
│   │   │   ├── tabs.ts               # 533行 — 集中式 TabManager 编排器
│   │   │   ├── password-capture.ts   # 570行 — 含 290行内联脚本
│   │   │   ├── session-manager.ts    # 221行 — 含 115行内联 SWFObject
│   │   │   ├── password-store.ts     # 456行
│   │   │   └── ...
│   │   └── utils/                     # 7 个工具模块
│   │
│   ├── renderer/                      # React UI（Vite 打包）
│   │   ├── App.tsx                    # 314行 — 根组件
│   │   ├── store/                     # Zustand stores
│   │   ├── hooks/                     # 5 个 React hooks（useTabManager 557行）
│   │   ├── services/                  # 11 个服务模块
│   │   ├── components/                # 11 个组件组
│   │   │   ├── automation/            # AutomationBlocklyEditor 550行
│   │   │   ├── userscripts/           # UserscriptsPage 729行
│   │   │   ├── panels/               # SettingsPanel 580行
│   │   │   └── ...
│   │   └── i18n/                      # typesafe-i18n 生成代码
│   │
│   ├── preload/                       # 主窗口 preload（contextBridge）
│   ├── webview-preload/               # BrowserView preload（Ruffle 注入 + 用户脚本运行时）
│   └── shared/                        # 主/渲染共享类型（15 文件）
│
├── build/                             # 电子构建器配置
├── scripts/                           # 构建辅助脚本
├── tests/                             # 43 单元测试 + 17 用户脚本测试 + 40 冒烟测试
├── tools/                             # 探测工具包 + 自动化演示
├── plugins/                           # 平台特定 PPAPI Flash 插件
├── native/                            # 原生二进制（aria2、鼠标钩子）
├── vendor/                            # Flash 二进制
├── assets/                            # 图片、字体
└── docs/                              # 架构手册、设计规范（50+ 文件）
```

### 1.3 完整功能模块拆分清单

| 模块名称 | 存放位置 | 核心职责 | 对外依赖 |
|----------|----------|----------|----------|
| **主进程入口** | `src/main/index.ts` | 单实例锁、窗口创建、IPC 注册、崩溃恢复 | 全部 modules |
| **标签页管理** | `src/main/modules/tabs.ts` | BrowserView 生命周期、导航、密码填充编排、右键菜单 | flash, session-manager, screenshot, password-*, userscripts |
| **Flash 引擎** | `src/main/modules/flash.ts` | PPAPI 插件路径解析、版本提取、mms.cfg 写入 | 无外部依赖 |
| **会话管理** | `src/main/modules/session-manager.ts` | UA 设置、SWFObject 重定向、CORS 头注入、Web 请求拦截 | flash |
| **截图** | `src/main/modules/screenshot.ts` + `screenshot-http.ts` | 标签页截图捕获、调试 HTTP 端点 | tabs |
| **密码捕获** | `src/main/modules/password-capture.ts` | CDP 密码表单检测、凭证捕获（290行内联脚本） | tabs |
| **密码填充** | `src/main/modules/password-fill.ts` | 跨帧密码自动填充 | password-capture |
| **密码保险库** | `src/main/modules/password-store.ts` | 加密存储、设备密钥包装 | 无外部依赖 |
| **用户脚本管理器** | `src/main/modules/userscripts/userscript-manager.ts` | 脚本索引、视图注册、快照构建、GM 值存储 | matcher, store, require-cache |
| **用户脚本运行时** | `src/webview-preload/userscripts/` | GM API 构建、沙箱执行、调度器、页面桥接 | main/ipc（通过 IPC） |
| **自动化** | `src/main/modules/automation/` + `src/renderer/components/automation/` | Blockly 工作流、BrowserView 驱动、视觉工作线程 | tabs, userscripts |
| **配置** | `src/main/modules/config.ts` | electron-store 配置读写 | 无外部依赖 |
| **会话恢复** | `src/main/modules/session-recovery.ts` | 干净/异常关闭追踪 | 无外部依赖 |
| **渲染入口** | `src/renderer/App.tsx` | React 根组件、布局编排 | store, hooks, components |
| **数据存储** | `src/renderer/store/useDataStore.ts` | 收藏/历史/下载/设置/密码/主题/面板/Toast 状态 | Dexie/IndexedDB |
| **标签管理 Hook** | `src/renderer/hooks/useTabManager.ts` | 标签 CRUD、会话恢复、历史记录、缩放、IPC 监听 | electronAPI, useDataStore, useTabsStore |
| **Preload 桥接** | `src/preload/index.ts` | contextBridge IPC 白名单、通道安全封装 | ipcRenderer |

---

## 第二章：架构与设计问题检查

### 2.1 上帝类 / 充血大类

| 问题编号 | 文件路径 | 问题类型 | 风险等级 | 问题详细描述 | 优化建议 |
|----------|----------|----------|----------|-------------|----------|
| A-001 | `src/main/modules/tabs.ts` | 集中式编排类 | **中** | `TabManager` 533行并承担多项职责，但其中 BrowserView 生命周期、current-WebContents 事件守卫、导航前 CDP teardown 和 userscript 注册存在强时序关系。仅凭行数不能认定为生产故障。 | 在有测试覆盖的前提下渐进提取纯逻辑或无状态协作者；优先提取右键菜单、页面元数据等低耦合部分，不做一次性拆分。 |
| A-002 | `src/main/modules/userscripts/userscript-manager.ts` | 集中式编排类 | **中** | `UserscriptManager` 639行并覆盖多个职责域，但还负责 view 注册、后台脚本独立窗口和值广播的一致性。属于可维护性债务，尚无证据表明它构成高风险运行时问题。 | 先提取快照公共构建逻辑和值读取辅助函数；其余边界随功能修改渐进拆分，并保留生命周期集中协调。 |
| A-003 | `src/renderer/hooks/useTabManager.ts` | 大型 Hook | **中** | 557行，包含标签生命周期、会话、历史、IPC 与缩放逻辑。复杂度较高，但当前类型检查和测试均通过，不能仅按 hook/ref 数量定为高风险。 | 优先提取具有明确输入输出的 `useTabHistory`、`useTabZoom`；生命周期与 IPC 拆分必须补充行为测试。 |
| A-004 | `src/renderer/components/userscripts/UserscriptsPage.tsx` | 大型组件 | **中** | 729行并承担列表、编辑、安装确认和值面板等视图。主要风险是维护成本，而非已确认的生产故障。 | 按稳定的视图边界渐进提取子组件；无需为此额外引入路由或状态机，除非交互复杂度确有增长。 |
| A-005 | `src/renderer/store/useDataStore.ts` | 上帝 Store | **中** | 240行，包含10个状态切片：收藏、历史、下载、主题、设置、密码、密码存储状态、活动面板、Toast 队列。跨越书签、历史、下载、主题、设置、密码、导航、通知8个不相关业务域。 | 拆分为：`useFavoritesStore`、`useHistoryStore`、`useDownloadsStore`、`useSettingsStore`、`usePasswordStore`、`useUIStore`（面板/Toast/主题）。 |

### 2.2 单一职责原则违背、模块边界模糊

| 问题编号 | 文件路径 | 问题类型 | 风险等级 | 问题详细描述 | 优化建议 |
|----------|----------|----------|----------|-------------|----------|
| A-006 | `src/main/modules/tabs.ts:338-356` | 菜单文案硬编码 | **低** | 右键菜单标签硬编码为中文（`'↩ 后退'`、`'↪ 前进'`、`'⟳ 刷新'`、`'📷 截图'`），与 renderer i18n 系统脱节。当前桌面应用以中文用户为主，实际影响有限。 | 后续扩展桌面菜单语言时统一接入主进程可用的翻译资源；无需仅为此提前拆出完整菜单框架。 |
| A-007 | `src/main/modules/tabs.ts:222-241` | 职责混合 | **中** | `refreshPageMetadata` 在标签管理器中执行 JavaScript 提取标题和 favicon，混入了表现层关注点。 | 提取为独立的 `PageMetadataExtractor` 模块。 |
| A-008 | `src/main/modules/tabs.ts:268-273` | 职责混合 | **中** | SWF 文件的内联 CSS 注入逻辑直接嵌入标签管理器。 | 提取为 `SwfStyleInjector`。 |
| A-009 | `src/main/modules/password-capture.ts` + `password-fill.ts` | 边界模糊 | **中** | 密码捕获和密码填充共享上下文 ID（`getCaptureContextIds`），填充模块直接耦合捕获模块的上下文。 | 明确共享的 `PasswordContextRegistry` 接口，两个模块通过接口交互而非直接依赖。 |
| A-010 | `src/renderer/components/panels/SettingsPanel.tsx` | 状态分裂 | **中** | 使用两个独立的 useState 对象（`form` + `mainForm`）管理同一设置域，保存时分别调用 `setSettings(form)` + `invoke('save-config', mainForm)`，可能导致部分保存成功。 | 统一为单一设置状态对象，使用单个保存操作保证原子性。 |

### 2.3 循环依赖、包划分混乱、接口抽象缺失

| 问题编号 | 文件路径 | 问题类型 | 风险等级 | 问题详细描述 | 优化建议 |
|----------|----------|----------|----------|-------------|----------|
| A-011 | `src/main/modules/userscripts/` | 依赖图 | **低** | 用户脚本模块依赖图为干净的 DAG，无循环依赖。`userscript-background.ts` 仅以类型方式导入 `UserscriptManager`。 | 无需修改，保持现状。 |
| A-012 | `src/main/modules/config.ts:83-121` | 配置字段重复映射 | **中** | `loadConfig()` 和 `saveConfig()` 手动逐字段复制，新增字段需要同步更新类型、默认值、schema 和读写映射。 | 建立单一字段描述表并由其生成 schema/读写映射；不要直接展开 `store.store`，以免绕过白名单、默认值与迁移约束。 |
| A-013 | `src/main/ipc/userscripts.ipc.ts` + `userscripts-admin.ipc.ts` | IPC 协议差异 | **信息** | 同步文档启动通道使用 listener/sendSync，管理页异步操作使用 handler/invoke，这是由调用协议决定的差异，不是可直接统一的缺陷。 | 保持协议差异；统一命名、Zod 严格模式和错误返回格式即可。尤其不能把 `userscript:get-config` 改成异步 handler。 |

### 2.4 重复代码、复制粘贴代码块

| 问题编号 | 文件路径 | 问题类型 | 风险等级 | 问题详细描述 | 优化建议 |
|----------|----------|----------|----------|-------------|----------|
| A-014 | `userscript-manager.ts:141-203` vs `232-294` | 代码重复 | **中** | `snapshotFor()` 和 `snapshotBackground()` 在脚本载荷、资源预算和值快照构建方面明显重复，但二者的注册校验与过滤语义不同。 | 提取只负责载荷、资源和值组装的私有辅助函数；前台/后台入口继续显式保留各自过滤条件。 |
| A-015 | `userscript-manager.ts:405-412` vs `416-423` | 代码重复 | **中** | `getValuesFor()` 和 `listScriptValues()` 逻辑几乎相同，唯一区别是 `views.has()` 检查。 | 合并为一个方法，用参数控制视图过滤。 |
| A-016 | `password-capture.ts` 内联脚本内 | 代码重复 | **中** | `CAPTURE_SCRIPT` 中密码参数提取逻辑分散在 URL-encoded 与 JSON 请求体策略中，容易在扩展参数别名时产生行为差异；这是维护风险，不是已确认的运行时高风险。 | 提取不依赖 CDP/DOM 的纯解析函数，并用 URL、form 与 JSON 样例做回归测试。 |
| A-017 | `password-capture.ts:472` vs `561` | 代码重复 | **中** | LRU 驱逐逻辑在两个位置重复：`state.capturedSet.size > 200` 检查和清理。 | 提取为 `evictCapturedSet(state)` 函数。 |
| A-018 | `password-store.ts:130-136` vs `330-336` | 代码重复 | **中** | 密码验证逻辑（大写+小写+数字）在 `setupMaster` 和 `changeMaster` 中重复。`MIN_PASSWORD_LENGTH = 8` 也重复定义。 | 提取为 `validatePasswordStrength(pw: string): { valid: boolean; reason?: string }`。 |
| A-019 | `tabs.ts:528-530` vs `password-capture.ts:53-56` | 代码重复 | **中** | `sendToRenderer` / `private send` 模式完全相同：获取主窗口、检查 destroyed、发送。 | 提取为共享的 `sendToMainWindow(channel, payload)` 工具函数。 |
| A-020 | `FavoritesPanel.tsx:14` vs `HistoryPanel.tsx:15` vs `NewTabPage.tsx:22` | 代码重复 | **低** | `getHost(url)` 辅助函数在三个组件中完全相同地定义。 | 提取到 `src/renderer/services/url-utils.ts`。 |
| A-021 | `useTabManager.ts:14-21` vs `url-utils.ts:1-5` | 代码重复 | **低** | `NEWTAB_URL`、`USERSCRIPTS_URL`、`AUTOMATION_URL` 常量和 `isNewtabUrl()` 函数在 hook 中重复定义。 | 从 `url-utils.ts` 导入。 |
| A-022 | `SettingsPanel.tsx` 多处 | 代码重复 | **中** | 相同的 toggle-switch JSX 结构重复5次（lines 293-301, 308-316, 368-378, 479-487, 499-507）。 | 提取为 `<Toggle checked={...} onChange={...} label={...} />` 组件。 |

### 2.5 不合理继承、臃肿工具类

| 问题编号 | 文件路径 | 问题类型 | 风险等级 | 问题详细描述 | 优化建议 |
|----------|----------|----------|----------|-------------|----------|
| A-023 | `src/renderer/components/automation/AutomationBlocklyEditor.tsx` | 双向序列化共享约束不足 | **中** | `compileBlock` 和 `createStep` 承担双向转换；简单标量步骤可以共享字段 schema，但拖拽、条件和嵌套序列具有分支语义，不适合强行采用同一种通用转换器。 | 用完整的 step→block 类型表提供编译期穷尽检查；简单步骤共享读写 schema；复杂步骤保留显式分支并逐步增加往返测试。 |
| A-024 | `src/main/modules/userscripts/userscript-store.ts:80-89` | 同步 I/O 阻塞 | **中** | `save()` 使用 `writeFileSync`/`renameSync`，在值存储较大时阻塞主进程事件循环。 | 改用 `fs.promises.writeFile` + `fs.promises.rename`（异步版本）。 |

**已核验的非问题：** 文件名清理逻辑目前已经统一。`download.ts`、`screenshot.ts` 使用
`src/main/utils/download-path.ts` 的 `sanitizeDownloadFilename()`；
`userscript-download.ts` 仅将同一函数重导出为 `sanitizeFileName`，不存在两套实现。

---

## 第三章：性能风险点扫描

| 问题编号 | 文件路径 | 问题类型 | 风险等级 | 问题详细描述 | 优化建议 |
|----------|----------|----------|----------|-------------|----------|
| P-001 | `src/renderer/hooks/useTabManager.ts:60` | 最新值 ref 同步 | **信息** | 无依赖数组的 effect 用于同步少量 ref，是规避闭包陈旧值的常见模式；每次渲染的几次赋值没有可见性能风险。 | 保持现状；若后续拆分 hook，可随结构调整减少 ref，但不单独整改。 |
| P-002 | `src/renderer/store/useDataStore.ts:168` | JSON 比较 | **中** | `JSON.stringify(previous) === JSON.stringify(item)` 用于下载变更检测，键顺序依赖且对大对象昂贵。 | 使用浅比较或 Zustand 的 `shallow` 比较器。 |
| P-003 | `src/renderer/components/userscripts/UserscriptsPage.tsx:366` | 无缓存计算 | **低** | `filtered` 列表在每次渲染时重新计算，无 `useMemo`。 | 包装为 `useMemo(() => filterScripts(...), [scripts, searchTerm])`。 |
| P-004 | `src/renderer/App.tsx:240` | 渲染时数组扫描 | **低** | `favorites.some()` 在渲染期间扫描完整收藏列表，每渲染执行两次。 | 使用 `useMemo` 缓存或转为 `Set` 查找。 |
| P-005 | `src/renderer/components/panels/SettingsPanel.tsx` | 全量 DOM 挂载 | **中** | 所有五个设置分区始终挂载在 DOM 中（通过 CSS `display` 切换隐藏），隐藏分区仍参与 React 协调。 | 使用条件渲染（`{activeSection === 'general' && <GeneralSection />}`）。 |
| P-007 | `src/renderer/App.tsx:218-240` | 内联函数 props | **中** | 传递给 `TopBar` 的 `onBack`、`onForward`、`onStop`、`onReload`、`onToggleBookmark` 均为内联箭头函数，导致子组件每次渲染都收到新引用。 | 包装为 `useCallback`。 |
| P-008 | `src/main/modules/screenshot.ts:108` | 内存风险 | **中** | `image.toPNG().toString('base64')` 将完整 PNG 转为 base64 字符串，大截图可能消耗大量内存。无大小限制。 | 添加最大尺寸检查，或流式处理。 |
| P-009 | `src/main/modules/screenshot.ts:129-134` | 同步 IO | **低** | `getScreenshotDir()` 使用 `accessSync` 同步文件系统访问。 | 改为异步 `fs.promises.access`。 |
| P-010 | `src/main/modules/password-capture.ts:39-51` | 进程级定时器生命周期 | **低** | 模块级 TTL 定时器会存活至主进程退出；正常 Electron 单例模块不会重复创建。测试隔离或热重载场景下才可能需要显式清理。 | 可将定时器封装为进程级服务并提供测试清理钩子；不应在单个 `teardownCapture` 中清理共享定时器。 |
| P-011 | `src/main/modules/password-capture.ts:472` | 集合上限偏差 | **低** | 该集合不是无界的；当前在添加前仅检查 `> 200`，稳态最多保留约201项。 | 若要求严格上限200，在添加前使用 `>= 200` 驱逐最旧项，并用测试锁定边界。 |
| P-012 | `src/renderer/components/automation/AutomationPage.tsx:266` | 频繁事件监听器重绑 | **低** | 键盘快捷键 useEffect 依赖 `[dirty, jsonDirty, selectedId, workflow]`，JSON 编辑器每次击键都重新绑定事件监听器。 | 使用 ref 追踪最新状态，effect 仅依赖空数组。 |
| P-013 | `src/renderer/components/panels/ThemeToggle.tsx` | requestAnimationFrame 重渲染 | **低** | 动画使用 `requestAnimationFrame` + `setProgress` 逐帧触发 React 重渲染。 | 改为 CSS 动画或 ref-based 方案。 |

> 撤销原 P-006：“整个 renderer 零 React.memo”是错误搜索结论，`TabItem.tsx` 已使用
> `memo(TabItem)`；同时没有 Profiler 数据证明存在全局重渲染问题。后续性能优化应先测量，
> 不再把是否使用 `memo` 单独登记为问题。

---

## 第四章：BUG 隐患、安全、健壮性问题

| 问题编号 | 文件路径 | 问题类型 | 风险等级 | 问题详细描述 | 优化建议 |
|----------|----------|----------|----------|-------------|----------|
| B-001 | `src/main/modules/tabs.ts:498` | 类型安全 | **中** | `findInPage` 接受 `options?: any` — 无类型约束的参数。 | 定义 `FindInPageOptions` 接口。 |
| B-002 | `src/main/modules/tabs.ts:400` | 脆弱类型转换 | **中** | `(wc as unknown as { destroy(): void }).destroy()` 双重类型转换绕过 Electron 11 类型限制。 | 定义明确的类型断言接口。 |
| B-003 | `src/main/modules/password-capture.ts:429` | 类型建模不精确 | **低** | `globalPendingCredentials` 的值比 `CaptureState.pendingCredentials` 多一个 `timestamp` 字段，因此使用了 `as any`。该 Map 仍是模块私有，没有因此暴露全局状态或绕过外部安全边界。 | 定义共享的 `PendingCredential` 类型并移除断言。 |
| B-004 | `src/main/modules/password-capture.ts:474` | 弱 ID 生成 | **低** | `Math.random().toString(36).slice(2, 8)` — 6字符 base36，约21亿种组合，碰撞概率不为零。 | 改用 `crypto.randomUUID()` 或 `nanoid`（项目已有依赖）。 |
| B-005 | `src/main/modules/password-store.ts:37-64` | 顶层存储实例 | **中** | 两个 `electron-store` 在模块加载时实例化，会增加测试隔离和初始化顺序耦合；但 AGENTS.md 的强制惰性要求特指会进入 userscripts 无 Electron 测试导入链的 `config.ts`。当前 514 项 Vitest 均通过，不能认定这里已造成导入崩溃。 | 在补充 password-store 隔离测试后考虑惰性工厂；不要以“违反 AGENTS.md”作为修复依据。 |
| B-006 | `src/main/modules/password-store.ts:258` | 语义错误 | **中** | `deleteEntry` 始终返回 `true`，无论条目是否存在。调用方无法区分删除成功和条目不存在。 | 改为返回 `boolean` 表示是否实际删除了条目。 |
| B-007 | `src/main/modules/session-manager.ts:174-185` | 已接受的兼容性策略 | **信息** | CORS 头只对 `.swf` 响应注入，用于 Ruffle 跨域 fetch；这是 AGENTS.md 明确记录的兼容策略，不等同于全局 CORS 放开。贸然改成站点白名单会造成旧游戏白屏。 | 保持现状并补充回归测试。若未来收紧，必须先建立真实站点兼容矩阵，并区分 Ruffle 与 PPAPI 会话。 |
| B-008 | `src/main/modules/password-capture.ts:523-526` | 异常吞没 | **中** | `Runtime.evaluate` 的 catch 块为空：`catch(() => {})` — 可能隐藏关键注入失败。 | 至少记录 debug 级别日志。 |
| B-009 | `src/main/modules/tabs.ts` 多处 | 异常吞没 | **中** | 至少8个空 `catch {}` 块（lines 229, 240, 274, 307, 399-401, 524），部分有意为之（"不得中断导航"），但使调试困难。 | 对非关键操作保留空 catch 并添加注释；对关键操作添加 debug 日志。 |
| B-010 | `src/main/modules/password-store.ts:348-361` | 原子性风险 | **中** | `resetAll` 先 `store.clear()` 再逐个 `store.set()`，如果中间失败会导致状态不一致。 | 使用事务性操作或 `store.store = { ... }` 单次赋值。 |
| B-011 | `src/main/ipc/userscripts.ipc.ts:224-231`、`src/main/ipc/userscripts-admin.ipc.ts:249-253` | IPC 值深度未显式限制 | **低** | IPC 入口使用 `z.unknown()` 后断言为 `GMSerializable`。下游 `UserscriptValueStore.set()` 已检查循环引用、JSON 可序列化性和16KB大小上限，并捕获失败，实际攻击面较小；剩余缺口是递归校验没有显式最大深度。 | 有安全加固迭代时在共享的 `isSerializableValue` 中增加深度/节点预算，并补充深层对象回归测试。 |
| B-013 | `src/renderer/components/panels/SettingsPanel.tsx:31-44` | 配置不同步 | **中** | `DEFAULT_MAIN_CONFIG` 复制了主进程配置模式，如果主进程模式变更，此副本会静默过时。 | 通过 IPC 从主进程获取默认值。 |
| B-015 | `src/renderer/components/layout/TopBar.tsx:48` | 废弃属性 | **低** | `isDark` prop 被解构后立即重命名为 `_isDark`，从未使用。 | 删除该 prop。 |
| B-016 | `src/main/modules/tabs.ts:209` | 冗余空值合并 | **低** | `(this.userscriptGeneration = (this.userscriptGeneration ?? 0) + 1)` — `?? 0` 是死代码，因为 `userscriptGeneration` 在 line 52 已初始化为 `0`。 | 简化为 `this.userscriptGeneration++`。 |
| B-017 | `src/main/modules/index.ts:149-157` | 死代码 | **低** | `win.on('resize')` 和 `win.on('move')` 事件处理器为空函数体，仅有注释。 | 删除空处理器或实现它们。 |
| B-018 | `src/main/modules/userscripts/userscript-cookie-service.ts:63,80` | 模式不一致 | **低** | 硬编码 `session.fromPartition('persist:')` 而非像其他服务一样通过构造参数注入。 | 统一为构造参数传入 session。 |
| B-019 | `src/renderer/components/automation/AutomationBlocklyEditor.tsx:490-498` | 可接受的临时存储 | **信息** | `localStorage` 只持久化编辑器草稿；草稿属于可丢失临时态，不要求与正式工作流数据采用相同持久化层。 | 保持现状。只有出现跨设备、版本迁移或草稿恢复需求时再迁移到 Dexie。 |
| B-020 | `src/main/modules/password-capture.ts:32,36` | 模块级进程状态 | **低** | `globalPendingCredentials` 和 `shownToastKeys` 都是模块级 Map，用于跨 CDP detach/re-attach 保留待保存凭据并抑制重复 toast，这是有意的进程级语义；它会增加隔离测试清理成本，但不使单元测试“不可能”。 | 提供仅供测试/关闭流程使用的清理钩子，或封装为显式的进程级服务；不得改成随单个 capture state 销毁。 |
| B-021 | `src/renderer/store/useDataStore.ts:62,85-89` | 同步重入边界 | **低** | `hydrateFromDb()` 在同一同步调用栈内设置标志、调用 `setState` 并恢复标志，普通用户事件无法在其间异步插入，因此原报告描述的水合/用户操作竞态不成立。仅同步订阅器重入时可能误跳过持久化。 | 保持现状或改为显式的“不持久化水合”内部 action；只有出现同步重入复现时才需要 epoch 方案。 |

---

## 第五章：项目目录与包结构审计

### 5.1 根目录文件散落问题

**当前现状：**
根目录散落以下文件，未按功能归类：

| 文件 | 用途 |
|------|------|
| `mm.cfg` / `mms.cfg` | Flash Player 调试配置 |
| `launch.vbs` | Windows 启动脚本 |
| `provenance.json` | 构建指纹 |
| `eslint.config.mjs` | ESLint 配置 |
| `.prettierrc` | Prettier 配置 |
| `postcss.config.js` | PostCSS 配置 |
| `tailwind.config.js` | Tailwind 配置 |
| `.typesafe-i18n.json` | i18n 配置 |
| `vitest.config.ts` | 测试配置 |

**问题危害：** 根目录有10+配置文件，加上6个目录，视觉上杂乱。Flash 配置文件（`mm.cfg`、`mms.cfg`）与项目配置混在一起。

**优化方案：** Flash 配置文件移入 `config/flash/` 目录；构建相关配置（postcss、tailwind、eslint、prettier、vitest、typesafe-i18n）保留在根目录（Node.js 工具链约定要求）；`launch.vbs` 移入 `scripts/`。

### 5.2 目录划分依据混乱

**当前现状：**
- `src/main/modules/` 按功能划分（tabs、flash、session-manager、password-*）
- `src/main/ipc/` 按技术层划分（IPC 处理器）
- `src/renderer/components/` 混合按功能（panels、overlays）和按业务（automation、userscripts）

**问题危害：** 分类标准不统一，开发者需要记住哪些模块在 `modules/`、哪些在 `ipc/`、哪些在 `components/`。

**优化方案：**
```
src/main/
├── core/                    # 核心浏览器功能
│   ├── tabs/               # TabManager + 导航 + 右键菜单
│   ├── flash/              # PPAPI 插件 + mms.cfg
│   ├── session/            # 会话管理 + 恢复
│   └── screenshot/         # 截图
├── security/                # 安全相关
│   ├── password-capture.ts
│   ├── password-fill.ts
│   └── password-store.ts
├── userscripts/             # 用户脚本系统（保持现状，已组织良好）
├── automation/              # 自动化系统
├── ipc/                     # IPC 注册（保持技术层划分）
└── utils/                   # 工具函数
```

### 5.3 模块职责与目录路径不匹配

| 问题编号 | 当前路径 | 问题描述 | 优化方案 |
|----------|----------|----------|----------|
| D-001 | `src/main/modules/tabs.ts` | `TabManager` 包含密码填充编排（`_attemptPasswordFill`、`fillPassword`），这不是标签页的职责。 | 密码填充编排逻辑移入 `security/password-orchestrator.ts`。 |
| D-002 | `src/main/modules/password-capture.ts` | **部分完成。** URL/form 参数提取已拆到 `password-capture-params.ts` 并由快速单测覆盖；页面 binding、DOM submit 和 fetch hook 由 jsdom 测试覆盖，BrowserView smoke 验证真实生命周期。其余约束页面 hook 仍保留在内联脚本中。 | 后续按策略逐步提取，不一次性重写；始终保留至少一个 Electron smoke 验证 CDP binding 与真实执行上下文。 |
| D-003 | `src/renderer/components/panels/DownloadsPanel.tsx` | 291行，包含下载逻辑（格式化、暂停/恢复），混合了 UI 和业务逻辑。 | 下载操作逻辑提取到 `hooks/useDownloadManager.ts`。 |

### 5.4 子目录深度问题

**当前现状：** `src/webview-preload/userscripts/` 有6个文件，组织良好。`src/main/modules/userscripts/` 有20个文件，深度合理。

**无严重子目录深度问题。**

### 5.5 相关业务文件分散

| 问题编号 | 文件分散情况 | 问题描述 | 优化方案 |
|----------|-------------|----------|----------|
| D-004 | URL 工具函数分散 | `url-utils.ts`（renderer/services）、`url-privacy.ts`（shared/utils）、`userscript-versions.ts:17-24`（hostOfMatchPattern）。 | 统一到 `shared/utils/url.ts`。 |
| D-005 | 密码相关文件跨三个目录 | `password-capture.ts`、`password-fill.ts`、`password-store.ts`（main/modules）+ `usePasswordListener.ts`（renderer/hooks）+ `password-form-observer.ts`（webview-preload）+ `passwords.ts`（shared/types）。 | 主进程密码模块已在同一目录；renderer 和 preload 部分保持原位（它们是各自进程的适配层）。 |
| D-006 | 配置文件跨多个位置 | `config.ts`（main/modules）+ `useDataStore.ts:settings`（renderer/store）+ `SettingsPanel.tsx:DEFAULT_MAIN_CONFIG`（renderer/components）。 | 统一配置模式：renderer 通过 IPC 获取主进程配置，不维护本地副本。 |

### 5.6 缺少通用公共目录

**当前现状：**
- `src/main/utils/` 存在但仅7个文件
- `src/shared/utils/` 仅1个文件（`url-privacy.ts`）
- 缺少统一的异常处理、常量定义、日志工具

**优化方案：**
```
src/shared/
├── constants/               # 新建：跨进程共享常量
│   ├── urls.ts             # NEWTAB_URL, USERSCRIPTS_URL, AUTOMATION_URL
│   ├── limits.ts           # 所有魔法数字集中管理
│   └── domains.ts          # 7k7k.com 等硬编码域名
├── utils/                   # 扩展：通用工具函数
│   ├── url.ts              # 合并 url-utils + url-privacy + hostOfMatchPattern
│   ├── send-to-main.ts     # 统一 sendToRenderer 模式
│   └── password-validation.ts  # 合并密码验证逻辑
└── types/                   # 保持现状
```

---

## 优先级整改清单

### 已确认高风险（立即修复）

本次源码复核没有确认需要立即实施的大规模高风险修复。原清单把文件长度、`as any`、
缺少 `memo` 和已明确接受的 SWF CORS 兼容策略混同为生产事故风险，证据不足。
如果 Electron smoke 在刷新专用 bundle 后出现稳定失败，应以对应复现、日志和测试作为新的高风险项，
而不是按文件行数升级风险。

### 建议执行窗口（待绑定项目 issue）

项目当前没有提供 sprint 名称或 issue 编号，本文不虚构关联项。下面的时间线是建议窗口；
排期时应把每一行绑定到真实 issue，并由负责人补充目标 sprint。

| 优先级 | 问题编号 | 文件 | 问题摘要 | 建议窗口 | 完成标准 |
|--------|----------|------|----------|----------|----------|
| 1 | 构建基线 | `release/`、`release/tests/` | 探针报告5类产物过期，smoke 可能测试旧 main/preload/renderer 或旧测试 bundle | 下一次 Electron smoke 之前 | 按目标 smoke 的专用构建脚本刷新产物；`npm run probe` 不再报告相关 STALE |
| 2 | P-008 | `screenshot.ts` | 返回完整 PNG base64 时没有响应大小预算 | 下一安全/稳定性 sprint | 明确最大像素或编码字节预算；超限返回结构化错误；保存路径不受影响 |
| 3 | A-014 | `userscript-manager.ts` | 前台/后台快照组装重复 | 下一次修改快照协议时 | 提取公共组装逻辑并保持前台 URL/noframes 与后台 per-script 过滤测试 |
| 4 | A-012 | `config.ts` | 配置字段在多处手工同步 | 下一次新增配置字段之前 | 单一字段描述来源，不绕过 schema、默认值或迁移约束 |
| 5 | P-005 | `SettingsPanel.tsx` | 隐藏设置分区仍保持挂载并参与协调 | 下一 UI 性能 sprint | 分区条件渲染；切换后表单状态和保存行为测试通过 |

### 计划性重构（有功能触达时进行）

| 优先级 | 问题编号 | 文件 | 问题摘要 | 前置条件 |
|--------|----------|------|----------|----------|
| 6 | A-001 | `tabs.ts` | 生命周期编排类较大 | 先覆盖引擎切换晚到事件、导航前 teardown、挂起恢复测试 |
| 7 | A-002 | `userscript-manager.ts` | userscript 协调器职责较多 | 先稳定 view/background/value/command 生命周期契约 |
| 8 | A-003 | `useTabManager.ts` | Hook 职责较多 | 先提取历史与缩放等低耦合逻辑，避免一次性重写 |
| 9 | A-004 | `UserscriptsPage.tsx` | 页面组件较大 | 以现有视图边界提取组件并保持安装/编辑状态测试 |
| 10 | A-005 | `useDataStore.ts` | 多业务域集中在一个 store | 明确跨 slice 原子更新和水合顺序后再拆分 |
| 11 | A-023 | `AutomationBlocklyEditor.tsx` | 双向序列化依赖多个 switch | 建立 schema 后加入全步骤往返测试 |

### 低风险（随手清理或先测量）

| 问题编号 | 文件 | 问题摘要 |
|----------|------|----------|
| B-003 | `password-capture.ts` | 统一 pending credential 类型，移除局部 `as any` |
| A-020 | 多个 Panel | `getHost()` 重复定义3次 |
| A-021 | `useTabManager.ts` | URL 常量重复定义 |
| A-022 | `SettingsPanel.tsx` | toggle-switch JSX 重复 |
| B-009 | `tabs.ts` | 为关键空 catch 添加受控 debug 日志；导航容错 catch 保留 |
| A-006 | `tabs.ts` | 右键菜单中文硬编码；与原 B-012 合并为同一事项 |
| B-015 | `TopBar.tsx` | 废弃 `_isDark` prop |
| B-017 | `index.ts` | 空事件处理器死代码 |
| B-011 | userscript IPC | 递归深度预算属于低风险纵深防御，有安全加固迭代时处理 |
| B-021 | `useDataStore.ts` | 仅在同步订阅器重入可复现时调整水合标志 |
| P-003 | renderer | 先用 Profiler 测量，再决定是否为列表计算增加 `useMemo` |
| P-009 | `screenshot.ts` | 非热路径同步 access 检查，可随异步接口重构处理 |
| P-010/P-011 | `password-capture.ts` | 进程级清理钩子与严格200项边界 |
| P-013 | `ThemeToggle.tsx` | RAF 状态更新需先测量动画开销 |

### 2026-08-27 实施结果

| 项目 | 状态 | 实施结果与验证 |
|------|------|----------------|
| 构建基线 | **完成** | main/renderer/preload 与 userscript runtime/admin bundle 已刷新；probe 的 build freshness 为 `OK` |
| B-006 | **完成** | `deleteEntry()` 对不存在条目返回 `false`，且不会重写存储；新增存在/不存在测试 |
| B-008 | **完成** | CDP context/main-frame 注入失败写入不含凭据和URL的 debug 日志；重试生命周期测试通过 |
| P-008 | **完成** | base64 返回增加16,777,216像素与16MiB PNG预算；超限返回 `DATA_TOO_LARGE`，保存缓冲复用 |
| B-011 | **完成** | GM值校验增加64层深度和10,000节点预算，保留循环检测、共享引用和16KB存储预算 |
| A-014/A-015 | **完成** | 前后台快照共享载荷/资源/值组装；前台/后台过滤和管理侧 view-gate 语义保持不变 |
| A-012 | **完成** | 配置读写键由 `DEFAULT_CONFIG` 生成，schema 使用完整键类型约束；新增一致性测试 |
| P-005 | **完成** | SettingsPanel 仅挂载当前分区；父级表单草稿在分区切换后保持；新增组件测试 |
| A-016 / D-002（首步） | **完成** | form/URL 与平面 JSON 凭据解析提取为纯函数，fetch/XHR/beacon 共用；修复 URL-encoded 密码被转小写的问题并覆盖参数别名 |
| A-018 | **完成** | 主密码长度与字符规则统一到 `validatePasswordStrength()`，setup/change 不再维护两份规则 |
| A-020/A-021 | **完成** | 三处 `getHost()` 及内部页 URL 常量/判断统一到 `url-utils.ts` |
| A-022 | **完成** | 五处设置开关统一为带可访问标签的 `ToggleSwitch`，组件行为测试通过 |
| A-023（首步） | **完成** | 建立覆盖全部可渲染步骤类型的 block 映射；7类标量步骤共用双向字段 schema，并加入往返与 key-combo 变体测试 |
| A-003（首步） | **完成** | 标签初始状态与站点引擎规则解析移至纯 service，覆盖精确域名、子域名、伪后缀及内部页初态测试 |
| P-003/P-004/P-007 | **完成** | userscript 过滤使用 `useMemo`；收藏状态只计算一次；TopBar 导航、停止、刷新、收藏与静音回调使用稳定引用 |
| B-001/B-009/B-016 | **完成** | `findInPage` 改用 Electron 明确类型；元数据执行失败增加不记录URL的 debug 诊断；移除 generation 冗余空值合并 |
| B-003/B-015/B-017 | **完成** | 移除错误的 pending credential 建模与 `as any`、废弃 `isDark` prop、主窗口空 resize/move 处理器 |
| P-011 | **完成** | 捕获去重集合改为添加前按 `>= 200` 驱逐，严格保持上限并覆盖去重/边界测试 |

验证结果：`npm run check`、`npm run test:electron`、`npm run test:compat`、`npm run test:ruffle`、
`npm run test:css-fixer`、`npm run test:userscripts-admin` 均通过；
userscript runtime smoke 的147项必选检查全部通过，4项可选真实脚本检查为1项通过、3项已知兼容性失败，
最终决策为 `CONTINUE`。

---

## 附录：架构质量结论

不再给出缺少统一量表和历史基线的 1–5 主观评分。项目当前也没有可引用的正式评分标准；
如需恢复数字评分，应先建立独立的审计评分规范，定义每个维度的可测指标、权重、证据要求和历史基线，
再由本文引用该规范。当前可验证结论如下：

| 维度 | 结论 | 证据/限制 |
|------|------|-----------|
| **类型与静态检查** | 基线健康 | 三套 TypeScript typecheck 通过；ESLint 0 error、34 warnings |
| **单元测试** | 基线健康 | 81个测试文件、514项测试通过 |
| **运行时验证** | 尚不充分 | 探针报告5类构建产物过期，需刷新目标 smoke bundle 后复核 |
| **兼容性约束** | 强且有文档 | Electron 11、PPAPI、Ruffle、CDP 与单 webRequest listener 均限制重构方式 |
| **可维护性** | 有明确改进空间 | 快照组装重复，大型 manager/hook/component 可渐进拆分 |
| **性能** | 缺少测量证据 | 不能从是否使用 `memo`、`useMemo` 或同步小型 I/O 单独推断用户可见瓶颈 |
| **安全边界** | 已有多层约束 | Zod、值序列化与大小预算存在；GM 值最大递归深度仍可加固 |

**综合判断：** 当前源码基线通过静态检查与单元测试；首要任务是恢复可置信的构建/运行时验证，
然后处理有明确边界和完成标准的加固项。大型架构拆分属于计划性维护，不应冒充“立即修复”的生产风险。
